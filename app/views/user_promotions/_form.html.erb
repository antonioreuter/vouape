<%= form_for(@user_promotion) do |f| %>
  <% if @user_promotion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_promotion.errors.count, "error") %> prohibited this user_promotion from being saved:</h2>

      <ul>
      <% @user_promotion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :promotion_id %><br />
    <%= f.text_field :promotion_id %>
  </div>
  <div class="field">
    <%= f.label :comments %><br />
    <%= f.text_area :comments %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
